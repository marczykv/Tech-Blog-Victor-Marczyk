<h1>Edit Post</h1>
<form class="post-form" action="/post/edit/{{post.id}}" method="POST">
    <input type="text" name="title" value="{{post.title}}">
    <textarea name="content">{{post.content}}</textarea>
    <button type="submit">Update Post</button>
</form>
